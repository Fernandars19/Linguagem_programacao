class for { // não há entrada de dados, por isso não precisou importar o scanner.
    public static void main (string [] args){
        system.out.printIn("contando com 'for' em Java:");

        for (int i = 1; i <=5; i++){
            system.out.printIn(i);
        }
    }
}